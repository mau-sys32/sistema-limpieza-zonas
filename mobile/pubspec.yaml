name: limpieza_app_movil
description: App móvil/web del sistema de limpieza por zonas.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  firebase_core: ^3.5.0
  image_picker: ^1.0.7
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  http: ^1.2.2
  go_router: ^14.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true